# 🔐 راهنمای مدیریت کاربران

## 📋 فهرست مطالب
1. [کاربر ادمین پیش‌فرض](#کاربر-ادمین-پیش‌فرض)
2. [ساخت کاربر ادمین جدید](#ساخت-کاربر-ادمین-جدید)
3. [تغییر رمز عبور](#تغییر-رمز-عبور)
4. [نقش‌های کاربری](#نقش‌های-کاربری)
5. [مدیریت کاربران از رابط کاربری](#مدیریت-کاربران-از-رابط-کاربری)

---

## 🎯 کاربر ادمین پیش‌فرض

برای ساخت کاربر ادمین پیش‌فرض:

```bash
python install-pro-buddy/backend/create_admin.py
```

**اطلاعات ورود:**
- 📱 شماره موبایل: `09123456789`
- 🔑 رمز عبور: `admin123`
- 🛡️ نقش: مدیر سیستم (Admin)

⚠️ **توجه:** حتماً پس از اولین ورود، رمز عبور را تغییر دهید!

---

## 🆕 ساخت کاربر ادمین جدید

### روش 1: از طریق رابط کاربری (توصیه می‌شود)
1. با حساب ادمین وارد شوید
2. به بخش **"کاربران"** بروید
3. روی **"افزودن کاربر"** کلیک کنید
4. نقش را **"مدیر سیستم"** انتخاب کنید
5. اطلاعات را پر کنید و ذخیره کنید

### روش 2: از طریق دیتابیس
اگر دسترسی به رابط کاربری ندارید، می‌توانید مستقیماً از دیتابیس کاربر بسازید:

```bash
python install-pro-buddy/backend/create_admin.py
```

---

## 🔑 تغییر رمز عبور

### روش 1: تعاملی (Interactive)
```bash
python install-pro-buddy/backend/reset_password.py
```

سپس شماره موبایل و رمز عبور جدید را وارد کنید.

### روش 2: با پارامتر
```bash
python install-pro-buddy/backend/reset_password.py 09123456789 newpassword123
```

**مثال:**
```bash
python install-pro-buddy/backend/reset_password.py 09123456789 admin456
```

---

## 👥 نقش‌های کاربری

### 🛡️ مدیر سیستم (Admin)
**دسترسی‌ها:**
- ✅ مشاهده داشبورد کامل
- ✅ مدیریت شرکا
- ✅ مدیریت سرمایه‌گذاران
- ✅ مدیریت موجودی
- ✅ ثبت فروش
- ✅ مدیریت مشتریان
- ✅ مدیریت اقساط
- ✅ مدیریت هزینه‌ها
- ✅ **مدیریت کاربران** (ساخت، ویرایش، حذف)

### 👔 شریک (Partner)
**دسترسی‌ها:**
- ✅ مشاهده داشبورد اختصاصی خود
- ✅ مشاهده سرمایه و سود شخصی
- ✅ مشاهده تراکنش‌های شخصی
- ❌ دسترسی به سایر بخش‌ها

---

## 💻 مدیریت کاربران از رابط کاربری

### افزودن کاربر جدید
1. وارد بخش **"کاربران"** شوید (فقط برای ادمین)
2. روی **"افزودن کاربر"** کلیک کنید
3. فرم را پر کنید:
   - نام و نام خانوادگی
   - شماره موبایل (11 رقم، شروع با 09)
   - رمز عبور (حداقل 4 کاراکتر)
   - نقش کاربری (مدیر سیستم یا شریک)
   - اگر شریک انتخاب شد، شریک مرتبط را انتخاب کنید
4. روی **"افزودن کاربر"** کلیک کنید

### ویرایش کاربر
1. در لیست کاربران، روی **"ویرایش"** کلیک کنید
2. اطلاعات مورد نظر را تغییر دهید
3. برای تغییر رمز عبور، رمز جدید را وارد کنید (اختیاری)
4. روی **"بروزرسانی کاربر"** کلیک کنید

### فعال/غیرفعال کردن کاربر
- روی دکمه **"فعال"** یا **"غیرفعال"** کلیک کنید
- کاربران غیرفعال نمی‌توانند وارد سیستم شوند

### حذف کاربر
1. روی دکمه **"حذف"** (سطل زباله) کلیک کنید
2. حذف را تأیید کنید

⚠️ **توجه:** نمی‌توانید آخرین ادمین را حذف کنید!

---

## 🔒 نکات امنیتی

1. **رمز عبور قوی:** همیشه از رمز عبور قوی استفاده کنید
2. **تغییر رمز پیش‌فرض:** حتماً رمز عبور پیش‌فرض را تغییر دهید
3. **محدود کردن دسترسی:** فقط به افراد مورد اعتماد نقش ادمین بدهید
4. **بررسی منظم:** به طور منظم لیست کاربران را بررسی کنید
5. **غیرفعال کردن:** به جای حذف، کاربران غیرضروری را غیرفعال کنید

---

## 📞 پشتیبانی

اگر مشکلی در مدیریت کاربران دارید:
1. لاگ‌های backend را بررسی کنید
2. از اسکریپت‌های Python برای بررسی دیتابیس استفاده کنید
3. در صورت نیاز، دیتابیس را backup بگیرید

---

## 🎓 مثال‌های کاربردی

### مثال 1: ساخت کاربر برای شریک جدید
1. ابتدا شریک را در بخش "شرکا" اضافه کنید
2. سپس در بخش "کاربران"، کاربر جدید بسازید
3. نقش را "شریک" انتخاب کنید
4. شریک مرتبط را از لیست انتخاب کنید

### مثال 2: تغییر رمز عبور فراموش شده
```bash
python install-pro-buddy/backend/reset_password.py 09123456789 newpass123
```

### مثال 3: بررسی لیست کاربران
```bash
python install-pro-buddy/backend/check_admin.py
```

---

**تاریخ بروزرسانی:** 1403/09/07
**نسخه:** 1.0.0
