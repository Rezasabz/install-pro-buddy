# طرح پیاده‌سازی ویژگی‌های جدید

## ✅ مرحله ۱: سیستم محاسبه سود جدید (تکمیل شد)

### فایل‌های ایجاد شده:
- ✅ `src/lib/profitCalculations.ts` - توابع محاسبه سود

### تغییرات لازم در فایل‌های موجود:

#### 1. `src/lib/store.ts`
- ✅ اضافه شدن `ProfitCalculationType`
- ✅ آپدیت `Sale` interface

#### 2. `src/pages/Sales.tsx`
**تغییرات مورد نیاز:**
- [ ] اضافه کردن state برای نوع محاسبه سود
- [ ] اضافه کردن state برای درصد سود دلخواه
- [ ] آپدیت formData
- [ ] اضافه کردن UI برای انتخاب نوع محاسبه
- [ ] آپدیت preview calculation
- [ ] آپدیت handleSubmit

**UI Components مورد نیاز:**
```tsx
<div>
  <Label>نوع محاسبه سود</Label>
  <Select value={profitCalculationType} onValueChange={setProfitCalculationType}>
    <SelectItem value="fixed_4_percent">سود ثابت ۴٪</SelectItem>
    <SelectItem value="monthly_4_percent_lda">سود ماهیانه ۴٪ روی باقیمانده (LDA)</SelectItem>
    <SelectItem value="custom_annual">سود دلخواه (از ۸٪ به بالا)</SelectItem>
  </Select>
</div>

{profitCalculationType === 'custom_annual' && (
  <div>
    <Label>درصد سود سالانه (حداقل ۸٪)</Label>
    <Input 
      type="number" 
      min="8" 
      step="0.5"
      value={customProfitRate}
      onChange={(e) => setCustomProfitRate(e.target.value)}
    />
  </div>
)}
```

---

## 📋 مرحله ۲: سیستم احراز هویت

### فایل‌های جدید مورد نیاز:
- [ ] `src/lib/auth.ts` - توابع احراز هویت
- [ ] `src/contexts/AuthContext.tsx` - Context برای مدیریت کاربر
- [ ] `src/pages/Register.tsx` - صفحه ثبت‌نام
- [ ] `src/pages/Login.tsx` - صفحه ورود

### تغییرات در فایل‌های موجود:
- [ ] `src/App.tsx` - اضافه کردن AuthProvider و route های جدید
- [ ] `src/lib/store.ts` - اضافه کردن User interface و userStore

### User Interface:
```typescript
export interface User {
  id: string;
  fullName: string;
  mobile: string; // نام کاربری
  password: string; // هش شده
  createdAt: string;
}
```

### Protected Routes:
- همه صفحات فعلی باید protected باشند
- اگر کاربر لاگین نکرده، به صفحه Login redirect شود

---

## 📄 مرحله ۳: خروجی PDF

### کتابخانه‌های مورد نیاز:
```bash
npm install jspdf jspdf-autotable
npm install @types/jspdf --save-dev
```

### فایل‌های جدید:
- [ ] `src/lib/pdfGenerator.ts` - تولید PDF
- [ ] `src/components/PDFButton.tsx` - دکمه دانلود PDF

### ویژگی‌های PDF:
- لوگوی شرکت (placeholder)
- مشخصات مشتری
- جدول اقساط کامل
- امضای مشتری و مدیر
- فونت فارسی (IRANSans یا Vazir)
- استایل حرفه‌ای

---

## 💰 مرحله ۴: مدیریت هزینه‌ها

### فایل‌های جدید:
- [ ] `src/pages/Expenses.tsx` - صفحه مدیریت هزینه‌ها
- [ ] `src/lib/store.ts` - اضافه کردن Expense interface و expensesStore

### Expense Interface:
```typescript
export interface Expense {
  id: string;
  date: string;
  type: string; // اجاره، حقوق، تبلیغات، ...
  amount: number;
  description: string;
  createdAt: string;
}
```

### UI Components:
- فرم ثبت هزینه
- لیست هزینه‌ها با فیلتر و جستجو
- نمودار هزینه‌ها (اختیاری)

### تغییرات در Dashboard:
- اضافه کردن کارت "مجموع هزینه‌ها"
- اضافه کردن کارت "سود خالص"
- فرمول: سود خالص = مجموع سود فروش - مجموع هزینه‌ها

---

## 🔄 ترتیب پیاده‌سازی پیشنهادی:

1. **اول: تکمیل سیستم محاسبه سود** (در حال انجام)
   - آپدیت Sales.tsx
   - تست کامل

2. **دوم: سیستم هزینه‌ها** (ساده‌تر از بقیه)
   - ایجاد Expenses page
   - آپدیت Dashboard

3. **سوم: سیستم احراز هویت** (نیاز به دقت بیشتر)
   - ایجاد Auth pages
   - Protected routes

4. **چهارم: خروجی PDF** (نیاز به کتابخانه خارجی)
   - نصب jspdf
   - ایجاد PDF generator

---

## ⚠️ نکات مهم:

1. **Migration داده‌ها**: فروش‌های قدیمی باید `profitCalculationType` پیش‌فرض بگیرند
2. **Backward Compatibility**: کد باید با داده‌های قدیمی کار کند
3. **Validation**: همه ورودی‌ها باید validate شوند
4. **Error Handling**: مدیریت خطاها در همه جا
5. **Loading States**: نمایش loading در عملیات طولانی
6. **Real-time Updates**: استفاده از DataContext برای sync

---

## 📊 تخمین زمان:

- مرحله ۱ (محاسبه سود): ۲-۳ ساعت
- مرحله ۲ (احراز هویت): ۳-۴ ساعت  
- مرحله ۳ (PDF): ۲-۳ ساعت
- مرحله ۴ (هزینه‌ها): ۱-۲ ساعت

**جمع کل**: ۸-۱۲ ساعت کار

---

## 🚀 شروع پیاده‌سازی

آیا می‌خواهید:
1. ادامه مرحله ۱ (محاسبه سود) - آپدیت Sales.tsx
2. شروع مرحله ۴ (هزینه‌ها) - ساده‌تر است
3. یا ترتیب دیگری؟
