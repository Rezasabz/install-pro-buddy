# 🎨 راهنمای نصب لوگو

## 📋 مراحل نصب لوگو

### 1️⃣ آماده‌سازی تصویر لوگو

**فرمت‌های پیشنهادی:**
- PNG (با پس‌زمینه شفاف) - توصیه می‌شود ✅
- SVG (برای کیفیت بالا)
- JPG (اگر پس‌زمینه شفاف نیاز ندارید)

**اندازه پیشنهادی:**
- حداقل: 200x200 پیکسل
- بهینه: 512x512 پیکسل
- حداکثر: 1024x1024 پیکسل

---

### 2️⃣ قرار دادن فایل لوگو

فایل لوگو را در پوشه `public` قرار دهید:

```
install-pro-buddy/
  └── public/
      └── logo.png    ← فایل لوگو را اینجا قرار دهید
```

**نام فایل:**
- `logo.png` (پیش‌فرض)
- یا `logo.svg` (اگر SVG استفاده می‌کنید)

---

### 3️⃣ تغییر نام فایل (اختیاری)

اگر می‌خواهید نام دیگری استفاده کنید، در فایل‌های زیر تغییر دهید:

**فایل 1: `src/components/Layout.tsx`**
```tsx
<img 
  src="/logo.png"    ← نام فایل را اینجا تغییر دهید
  alt="Majid Mobile Logo" 
  className="relative h-10 w-10 object-contain"
/>
```

**فایل 2: `src/pages/Login.tsx`**
```tsx
<img 
  src="/logo.png"    ← نام فایل را اینجا تغییر دهید
  alt="Majid Mobile Logo" 
  className="h-20 w-20 object-contain"
/>
```

---

### 4️⃣ تنظیم اندازه لوگو

#### در Header (Layout):
```tsx
className="relative h-10 w-10 object-contain"
         ↑ ارتفاع  ↑ عرض
```

اندازه‌های پیشنهادی:
- کوچک: `h-8 w-8`
- متوسط: `h-10 w-10` (پیش‌فرض)
- بزرگ: `h-12 w-12`

#### در صفحه Login:
```tsx
className="h-20 w-20 object-contain"
         ↑ ارتفاع  ↑ عرض
```

اندازه‌های پیشنهادی:
- کوچک: `h-16 w-16`
- متوسط: `h-20 w-20` (پیش‌فرض)
- بزرگ: `h-24 w-24`

---

### 5️⃣ تست کردن

1. فایل لوگو را در `public/logo.png` قرار دهید
2. سرور را ری‌استارت کنید:
   ```bash
   # توقف سرور (Ctrl+C)
   # شروع مجدد
   npm run dev
   ```
3. صفحه را رفرش کنید (F5)
4. لوگو باید در Header و صفحه Login نمایش داده شود

---

## 🎨 تنظیمات پیشرفته

### تغییر نام برند

در فایل `src/components/Layout.tsx`:
```tsx
<span className="hidden sm:inline-block text-lg font-bold">
  مجید موبایل    ← نام برند را اینجا تغییر دهید
</span>
```

در فایل `src/pages/Login.tsx`:
```tsx
<CardDescription>
  مجید موبایل - سیستم مدیریت فروش اقساطی    ← توضیحات را اینجا تغییر دهید
</CardDescription>
```

---

### استفاده از SVG

اگر فایل SVG دارید:

1. فایل را با نام `logo.svg` در `public` قرار دهید
2. در کد، `logo.png` را به `logo.svg` تغییر دهید

**مزایای SVG:**
- کیفیت بالا در تمام اندازه‌ها
- حجم کمتر
- قابلیت تغییر رنگ با CSS

---

### Fallback Icon

اگر لوگو پیدا نشد، به صورت خودکار آیکون موبایل نمایش داده می‌شود:

```tsx
onError={(e) => {
  e.currentTarget.style.display = 'none';
  e.currentTarget.nextElementSibling?.classList.remove('hidden');
}}
```

---

## 🔧 عیب‌یابی

### مشکل: لوگو نمایش داده نمی‌شود

**راه‌حل 1:** مسیر فایل را بررسی کنید
```
✅ درست: public/logo.png
❌ اشتباه: src/assets/logo.png
❌ اشتباه: public/images/logo.png
```

**راه‌حل 2:** نام فایل را بررسی کنید
- نام فایل باید دقیقاً `logo.png` باشد
- حروف کوچک و بزرگ مهم است

**راه‌حل 3:** Cache مرورگر را پاک کنید
- Ctrl+Shift+R (Windows/Linux)
- Cmd+Shift+R (Mac)

**راه‌حل 4:** سرور را ری‌استارت کنید
```bash
# توقف سرور
Ctrl+C

# شروع مجدد
npm run dev
```

---

### مشکل: لوگو کیفیت پایینی دارد

**راه‌حل:**
1. از تصویر با رزولوشن بالاتر استفاده کنید (حداقل 512x512)
2. از فرمت SVG استفاده کنید
3. از تصویر با پس‌زمینه شفاف (PNG) استفاده کنید

---

### مشکل: لوگو خیلی بزرگ یا کوچک است

**راه‌حل:** کلاس‌های اندازه را تغییر دهید:

```tsx
// کوچک‌تر
className="h-8 w-8"

// بزرگ‌تر
className="h-12 w-12"
```

---

## 📝 نکات مهم

1. ✅ از تصویر با کیفیت بالا استفاده کنید
2. ✅ پس‌زمینه شفاف (PNG) بهتر است
3. ✅ اندازه فایل را کم نگه دارید (زیر 100KB)
4. ✅ نسبت ابعاد مربع (1:1) بهتر است
5. ✅ از رنگ‌های متناسب با تم استفاده کنید

---

## 🎯 مثال کامل

```bash
# 1. فایل لوگو را کپی کنید
cp /path/to/your/logo.png install-pro-buddy/public/logo.png

# 2. سرور را ری‌استارت کنید
cd install-pro-buddy
npm run dev

# 3. مرورگر را باز کنید
# http://localhost:8080
```

---

**تاریخ بروزرسانی:** 1403/09/07
**نسخه:** 1.0.0
